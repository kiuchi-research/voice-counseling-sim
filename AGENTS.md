# AGENTS.md

- ユーザー向けの説明は日本語で行う。
- 本体は `src/` と `app/`、テストは `tests/`、設定は `config/`、補助スクリプトは `utility/` に置く。
- 実行前に `python utility/check_env.py` を実行する。依存関係の正本は `environment.yml`、起動手順は `README.md`。
- 変更は小さく保ち、変更に対応するテストから確認する。
- UI変更時は `DESIGN.md` を読む。
- カウンセラー／クライアントのプリセットは明示的な依頼なしに変更しない。
- LLMの判断を文字列ルールで置き換えたり、新しいフォールバックを追加したりしない。必要なら先に相談する。
- `.env`、認証情報、録音、対話履歴を出力・コミットしない。生成物は `results/`、一時作業は `workbench/` に置く。
- APIに接続する確認とAPI不要のテストを区別する。実APIの確認では架空の入力を使い、保存済み面接を送らない。
- プロジェクトの構造とテスト手順は `docs/development.md` を参照する。
