[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "counseling-voice-demo"
version = "0.1.0"
description = "Voice counseling simulation research demo."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
